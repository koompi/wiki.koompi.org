<!DOCTYPE html>
<html lang="en">
    <head>
        <% include!("./header.stpl"); %>
    </head>
    <body>
        <div id="root">
            <!-- Menu -->
            <% include!("./nav.stpl"); %> 
            <% include!("./footer.stpl"); %> 
            <% include!("./main.stpl"); %>
        </div>

        <!-- Footer area -->
        <script src="/public/main.js"></script>
        <% include!("./footer.stpl"); %>
    </body>
</html>
